# Direct dependencies — gerado por pip-compile (pip-tools)
# Edite este arquivo, nunca o requirements.txt diretamente.
# Para regenerar: pip-compile --generate-hashes requirements.in
#
# NOTA: sentence-transformers e torch (~800 MB) tornam a geração de hashes
# inviável em CI. Use requirements-test.in para o pipeline de testes.

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Data validation
pydantic==2.7.1
pydantic-settings==2.2.1

# Vector DB
qdrant-client==1.9.1

# Embeddings (runtime — mockados em testes)
sentence-transformers==3.0.0
torch==2.3.0

# Observability
opentelemetry-sdk==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0
opentelemetry-exporter-otlp-proto-grpc==1.24.0
prometheus-fastapi-instrumentator==7.0.0
prometheus-client==0.20.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0
